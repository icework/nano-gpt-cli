interface:
  display_name: "NanoGPT"
  short_description: "Use NanoGPT from local OpenClaw workflows"
  default_prompt: "Use NanoGPT through the local CLI wrappers to answer this request or generate an image."

